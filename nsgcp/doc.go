package nsgcp

/*
 * This package has nullstone-specific helpers to aid with GCP operations across the entire module
 * It is explicitly named `nsgcp` instead of `gcp` to prevent common conflicts with the `gcp` package in ../contracts/gcp/
 */
