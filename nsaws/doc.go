package nsaws

/*
 * This package has nullstone-specific helpers to aid with AWS operations across the entire module
 * It is explicitly named `nsaws` instead of `aws` to prevent common conflicts with the `aws` package in ../contracts/aws/
 */
